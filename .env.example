# Example environment variables for the Enterprise RAG System

# Path to store data like knowledge base files and vector stores
# Default is 'data' in the project root if not specified.
# This path is used as the base for other data paths like vector stores and temporary KB files.
# It can be an absolute path (e.g., /var/data/my_app) or relative to the project root (e.g., ./my_custom_data).
# DATA_STORE_PATH=./data_custom

# --- Other existing variables from the new settings ---
# DEBUG=False
# OPENAI_API_KEY=your_openai_api_key_here
# DATABASE_URL=sqlite:///./app_data.db
# VECTOR_STORE_SUBDIR=vector_stores # Subdirectory under DATA_STORE_PATH
# KB_TEMP_SUBDIR=knowledge_bases_temp # Subdirectory under DATA_STORE_PATH
